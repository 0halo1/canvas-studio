class Vector3D {
  x: number
  y: number
  z: number

  constructor(x: number, y: number, z: number) {
    this.x = x
    this.y = y
    this.z = z
  }

  // Add other vector methods as needed, e.g., add, subtract, multiply, etc.
}

export default Vector3D
